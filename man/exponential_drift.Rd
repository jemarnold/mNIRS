% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyse_exponential_drift.R
\name{exponential_drift}
\alias{exponential_drift}
\title{Exponential-drift function}
\usage{
exponential_drift(t, A, B, tau, slope, tau_mult, TD = NULL)
}
\arguments{
\item{t}{A numeric vector of the predictor variable (time).}

\item{A}{A numeric parameter for the starting baseline of the response
variable.}

\item{B}{A numeric parameter for the ending asymptote of the response
variable.}

\item{tau}{A numeric parameter for the time constant \code{tau} (\eqn{\tau})
of the exponential curve, in units of the predictor variable \code{t}.}

\item{slope}{A numeric parameter for the linear drift rate \code{dx/dt}
of the secondary phase, in response units per unit of the predictor
variable \code{t}.}

\item{tau_mult}{A numeric multiple of \code{tau} after \code{TD} at which the linear
drift begins: drift onset \code{TD + tau_mult * tau} (\code{TD = 0} when absent).}

\item{TD}{A numeric parameter for the time delay before the onset of
exponential response, in units of the predictor variable \code{t}. If \code{NULL}
(\emph{default}), a 3-parameter model without time delay is used.}
}
\value{
A numeric vector of predicted values the same length as the
predictor variable \code{t}.
}
\description{
Calculate a two-phase curve: a primary monoexponential response with a
secondary linear drift beginning near the asymptote.
}
\details{
5-parameter model:
\code{A + (B - A) * (1 - exp(-t / tau)) + slope * pmax(t - tau_mult * tau, 0)}

6-parameter model:
\code{A + (B - A) * (1 - exp(-pmax(t - TD, 0) / tau)) + slope * pmax(t - TD - tau_mult * tau, 0)}

The primary phase is a \code{\link[=monoexponential]{monoexponential()}} response toward the asymptote
\code{B}. The secondary linear drift is exactly zero before the onset
\code{TD + tau_mult * tau}.
}
\examples{
## create an exponential curve with late linear drift and random noise
set.seed(13)
t <- 1:180
x <- exponential_drift(
    t, A = 10, B = 100, tau = 12, slope = -0.5, tau_mult = 3, TD = 15
) + rnorm(length(t), 0, 2)
data <- data.frame(t, x)

## the drift onset multiple is held constant in the formula
model <- nls(
    x ~ SSexponential_drift(t, A, B, tau, slope, tau_mult = 3, TD),
    data = data,
    algorithm = "port",
    lower = c(-Inf, -Inf, 0, -Inf, 0),
    control = nls.control(warnOnly = TRUE)
)
summary(model)

y <- predict(model, data)

\donttest{
    if (requireNamespace("ggplot2", quietly = TRUE)) {
        ggplot2::ggplot(data, ggplot2::aes(t, x)) +
            theme_mnirs() +
            ggplot2::geom_point() +
            ggplot2::geom_line(ggplot2::aes(y = y))
    }
}

}
\seealso{
\code{\link[=analyse_kinetics]{analyse_kinetics()}}, \code{\link[=SSexponential_drift]{SSexponential_drift()}},
\code{\link[=monoexponential]{monoexponential()}}, \code{\link[=biexponential]{biexponential()}}
}
