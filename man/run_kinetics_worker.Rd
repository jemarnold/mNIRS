% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aanalyse_kinetics_helpers.R
\name{run_kinetics_worker}
\alias{run_kinetics_worker}
\title{Run a kinetics worker on one interval with its model fallback chain}
\usage{
run_kinetics_worker(
  method,
  data,
  args,
  nirs_quo,
  time_quo,
  interval_name,
  verbose,
  fallback = TRUE,
  env = rlang::caller_env()
)
}
\arguments{
\item{method}{Character; the canonical method name.}

\item{data}{A single \emph{"mnirs"} data frame (one interval).}

\item{args}{Named list of the worker's resolved arguments.}

\item{nirs_quo}{A quosure or character vector of channel names.}

\item{interval_name}{Character; the interval label.}

\item{verbose}{Logical. Default is \code{TRUE}. Display or silence (if \code{FALSE})
warnings and information messages helpful for troubleshooting. Ad
global default can be set via \code{options(mnirs.verbose = FALSE)}.}

\item{fallback}{Logical; attempt the fallback chain.}

\item{env}{The call recorded for condition reporting.}
}
\value{
The worker's attributed coefficient data frame.
}
\description{
Calls the interval worker of \code{method}, then for methods listed in
\code{kinetics_fallbacks} tests each channel's fit with the method's
\code{trigger}. Channels with a reason are refit by the fallback method
(recursively down the chain) with the arguments it takes, user-fixed
parameters carried over through \code{fix_map}, and per-channel maps keyed
to those channels only. Their coefficients, model, fitted values,
diagnostics, and resolved arguments are replaced by the fallback fit's,
and the fallback is warned about and recorded in the \code{warnings}
attribute. A row where every fit in the chain failed reports the last
method tried with \code{NA} coefficients.
}
\details{
Methods with a fallback report the fitting method per row in a \code{model}
coefficient column and the union of the chain's coefficient columns
(\code{NA} where a model has no such parameter), so intervals bind
regardless of which triggers fire.
}
\keyword{internal}
