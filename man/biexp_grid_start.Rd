% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aanalyse_kinetics_helpers.R
\name{biexp_grid_start}
\alias{biexp_grid_start}
\title{Grid search starting estimates for a biexponential fit}
\usage{
biexp_grid_start(x, t, tau_ratio = 2.5, TD = 0, n_tau = 64L)
}
\arguments{
\item{x}{A numeric vector of the response variable.}

\item{t}{A numeric vector of the predictor variable (time).}

\item{tau_ratio}{A numeric lower bound on \code{tau2 / tau1}.}

\item{TD}{A numeric vector of candidate time delays subtracted from \code{t}
before fitting; the RSS-minimising delay is kept.}

\item{n_tau}{Integer resolution of the time constant grid.}
}
\value{
A named list of starting estimates (\code{A}, \code{B1}, \code{tau1}, \code{B2},
\code{tau2}, \code{TD}) with the attained \code{rss}, or \code{NULL} if no grid point is
usable.
}
\description{
Deterministic seed for \code{\link[=SSbiexponential]{SSbiexponential()}} and \code{\link[=fit_biexp_ratio]{fit_biexp_ratio()}}. The
biexponential is linear in \code{A}, \code{B1} and \code{B2} once \code{tau1}, \code{tau2}, and
\code{TD} are held fixed, so the amplitudes are profiled out by least squares
at every point of a log-spaced grid of time constants crossed with the
candidate delays, rather than searched (cf. \code{\link[=monoexp_init]{monoexp_init()}}).
}
\details{
The grid is confined to the ridge \code{tau2 >= tau_ratio * tau1}. As \code{tau2}
approaches \code{tau1} the two exponential basis vectors coincide, the design
matrix becomes singular, and the amplitudes diverge with cancelling signs;
the ridge excludes that degenerate region.

Grid limits scale with the span of \code{t}, so the seed does not depend on the
time units of the data. One log-spaced grid serves both components, and one
exponential basis \code{exp(-t / tau)} serves every delay: on the active rows
\code{t > TD} the delayed basis is a column rescaling of it, and earlier rows
contribute exactly zero, so each delay's normal equations are suffix sums
over the shared basis. Accumulating those sums from the largest delay down
costs one Gram pass in total, rather than one per delay. Each candidate
pair is then solved in closed form from its own 3x3 sub-block, with no
further pass over the data. A delay whose every pair is rank-deficient is
skipped, not fatal.

No ordering constraint is placed on the asymptotes \code{B1} or \code{B2} relative
to \code{A}. NIRS responses may legitimately fall or rise in either direction,
so identifiability is enforced through the time constants alone. This is
not a \code{direction} mechanism, which steers only the fit window.
}
\keyword{internal}
