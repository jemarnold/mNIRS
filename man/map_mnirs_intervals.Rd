% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_data_list.R
\name{map_mnirs_intervals}
\alias{map_mnirs_intervals}
\title{Apply an mnirs function over each interval of a multi-interval input}
\usage{
map_mnirs_intervals(data, call, eval_env, env = rlang::caller_env())
}
\arguments{
\item{data}{A data frame of class \emph{"mnirs"} containing time series data
and metadata, a list of data frames, or a grouped data frame (see
\emph{Details}).}

\item{call}{The matched call from the user-facing function, re-evaluated
with \code{data} swapped for each interval data frame.}

\item{eval_env}{Environment in which to re-evaluate \code{call}, i.e. the
user-facing function's \code{parent.frame()}, so NSE arguments resolve
against the original caller.}

\item{env}{The calling environment or a defused call, used to report
errors and warnings as coming from the user-facing function rather
than the validator.}
}
\value{
A named list of processed \emph{"mnirs"} data frames, one per
interval.
}
\description{
Shared entry point for \verb{*_mnirs()} transformer functions accepting a list
of data frames or a grouped data frame. Normalises \code{data} to a named list
via \code{\link[=as_data_list]{as_data_list()}}, then re-evaluates the captured user-facing call once
per interval with \code{data} swapped, so all arguments (including NSE channel
expressions) are forwarded verbatim.
}
\section{Data input formats}{


\emph{{mnirs}} processing functions accept \code{data} in multiple formats:
\itemize{
\item A \strong{single \emph{"mnirs"} data frame} is processed and returned directly.
\item A \strong{list of \emph{"mnirs"} data frames}: each interval is processed
separately and returned as a named list.
\item A \strong{grouped \emph{"mnirs"} data frame}, e.g. with \code{dplyr::group_by()}: the
data frame is split by grouping levels and each group is processed as
a separate interval, returned as a named list.
}
}

\keyword{internal}
