% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aanalyse_kinetics_helpers.R
\name{fit_td_fallback}
\alias{fit_td_fallback}
\title{Fit a self-start model with time-delay fallback}
\usage{
fit_td_fallback(
  x_fit,
  t_fit,
  params,
  .a,
  fitter,
  fn,
  .nirs,
  time_channel,
  interval_name,
  env,
  retry = .a$use_TD && !"TD" \%in\% names(.a$fix)
)
}
\arguments{
\item{x_fit, t_fit}{Numeric vectors of the channel fit window.}

\item{params}{Character vector of parameter names in model order,
including \code{TD} when the channel fits the TD model.}

\item{.a}{The channel's resolved argument list (\code{use_TD}, \code{fix}).}

\item{fitter}{A function \verb{(.data, .params, on_error)} fitting \code{.params}
to a data frame with the response and time columns named per
\code{\link[=fit_names]{fit_names()}} and returning an \link[stats:nls]{nls} model or \code{NULL}.
\code{on_error(e)} reports
the condition \code{e} and returns \code{NULL}, so it doubles as a \code{\link[=tryCatch]{tryCatch()}}
error handler.}

\item{fn}{Symbol or character; the model fn named in the message.}

\item{.nirs}{Character; the channel name.}

\item{time_channel}{Character; resolved time column name.}

\item{interval_name}{Character; the interval label.}

\item{env}{The calling environment or a defused call, used to report
errors and warnings as coming from the user-facing function rather
than the validator.}

\item{retry}{Logical; attempt the reduced model when the TD fit fails.}
}
\value{
A list with \code{model} (or \code{NULL}), the \code{params} actually fit,
the logical row filter \code{keep}, and the fit \code{data} frame.
}
\description{
Shared attempt skeleton for the nls-based kinetics workers. The TD
model is flat at \code{A} before \code{TD}, so the pre-onset baseline anchors
\code{A}; the reduced model has no such region and diverges at \code{t < 0}, so
it is fit from \code{start_time} onward. An under-determined attempt is
rejected before it reaches \code{fitter}, and a failed TD fit falls back to
the reduced model without \code{TD} unless \code{TD} is user-fixed. Every failure
is reported through \code{\link[=warn_fit_failed]{warn_fit_failed()}}.
}
\keyword{internal}
