% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyse_biexponential.R
\name{biexponential}
\alias{biexponential}
\title{Biexponential function}
\usage{
biexponential(t, A, B, tau1, tau2, prop)
}
\arguments{
\item{t}{A numeric vector of the predictor variable (time).}

\item{A}{A numeric parameter for the starting baseline of the response
variable.}

\item{B}{A numeric parameter for the ending asymptote of the response
variable.}

\item{tau1}{A numeric parameter for the \emph{fast} time constant (\eqn{\tau_1}),
in units of the predictor variable \code{t}.}

\item{tau2}{A numeric parameter for the \emph{slow} time constant (\eqn{\tau_2}),
in units of the predictor variable \code{t}. By convention \code{tau1 <= tau2}.}

\item{prop}{A numeric parameter in \verb{[0, 1]} for the \emph{amplitude fraction}
carried by the fast component \code{tau1}; the slow component \code{tau2} carries
the remaining \code{1 - prop}.}
}
\value{
A numeric vector of predicted values the same length as the
predictor variable \code{t}.
}
\description{
Calculate a 5-parameter biexponential curve: a shared-asymptote response
split between a fast and a slow exponential component.
}
\details{
This model is fit by \code{\link[=analyse_kinetics]{analyse_kinetics()}} when \code{method = "biexponential"},
and by \code{\link[stats:nls]{stats::nls()}} via the self-starting wrapper \code{\link[=SSbiexponential]{SSbiexponential()}}.
\subsection{Model equation}{

\code{A + (B - A) * (prop * (1 - exp(-t / tau1)) + (1 - prop) * (1 - exp(-t / tau2)))}

The two components share the overall amplitude \code{B - A}; \code{prop} divides it
between the fast (\code{tau1}) and slow (\code{tau2}) time constants. \code{tau1} and
\code{tau2} are exchangeable, so a \code{tau1 <= tau2} ordering is used to make the
parameters identifiable. \code{prop} is clamped to \verb{[1e-6, 1 - 1e-6]} internally
to keep the fit stable during iteration.

The amplitude-weighted \emph{mean response time} is
\code{MRT = prop * tau1 + (1 - prop) * tau2}.
}
}
\examples{
## create a biexponential curve with random noise
set.seed(1)
t <- 0:120
x <- biexponential(t, A = 50, B = 80, tau1 = 5, tau2 = 40, prop = 0.6) +
    rnorm(length(t), 0, 0.8)
data <- data.frame(t, x)

model <- nls(x ~ SSbiexponential(t, A, B, tau1, tau2, prop), data = data)
summary(model)

y <- predict(model, data)

\donttest{
    if (requireNamespace("ggplot2", quietly = TRUE)) {
        ggplot2::ggplot(data, ggplot2::aes(t, x)) +
            theme_mnirs() +
            ggplot2::geom_point() +
            ggplot2::geom_line(ggplot2::aes(y = y))
    }
}

}
\seealso{
\code{\link[=analyse_kinetics]{analyse_kinetics()}}, \code{\link[=SSbiexponential]{SSbiexponential()}}, \code{\link[=monoexponential]{monoexponential()}}
}
