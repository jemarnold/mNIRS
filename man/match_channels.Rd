% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_mnirs_helpers.R
\name{match_channels}
\alias{match_channels}
\title{Match channels to data column names and make names unique}
\usage{
match_channels(channels, data_names, verbose = TRUE, env = rlang::caller_env())
}
\arguments{
\item{channels}{A list of \code{time}, \code{event}, \code{extra}, and \code{nirs} channel
mappings from \code{resolve_channels()}.}

\item{data_names}{Unique column names of the data table.}

\item{verbose}{Logical. Default is \code{TRUE}. Display or silence (if \code{FALSE})
warnings and information messages helpful for troubleshooting. Ad
global default can be set via \code{options(mnirs.verbose = FALSE)}.}

\item{env}{The calling environment or a defused call, used to report
errors and warnings as coming from the user-facing function rather
than the validator.}
}
\value{
\code{channels} with unique names and original values, \code{NULL}
elements dropped.
}
\description{
Original names are made unique to match \code{rename_duplicates(names(data))};
duplicated new names are made unique with a warning.
}
\keyword{internal}
