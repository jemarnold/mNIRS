% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyse_sigmoidal_drift.R
\name{sigdrift_start}
\alias{sigdrift_start}
\title{Starting estimates for the sigmoidal-drift model}
\usage{
sigdrift_start(x, t, fixed = list(), shape = "symmetric")
}
\arguments{
\item{x}{A numeric vector of the response variable (sorted by \code{t}).}

\item{t}{A numeric vector of the predictor variable.}

\item{fixed}{A named list of user-fixed parameter values.}

\item{shape}{Character; the sigmoidal shape. One of \code{"symmetric"}
(\emph{default}; \code{\link[=logistic]{logistic()}}), \code{"gompertz"} (\code{\link[=gompertz]{gompertz()}}), or
\code{"gompertz_left"} (\code{\link[=gompertz_left]{gompertz_left()}}).}
}
\value{
A named numeric vector of starting estimates in model order.
}
\description{
Vector-level initialiser behind \code{\link[=sigdrift_init]{sigdrift_init()}}, called directly by
the kinetics worker on the fit window. The sigmoid is seeded as for
\code{\link[=SSgompertz]{SSgompertz()}} (\code{\link[=init_asymptotes]{init_asymptotes()}}, \code{\link[=init_inflection]{init_inflection()}}), the cutoffs
resolved from that seed, and each tail's residual from the seeded model
regressed on time from its cutoff: the intercept corrects the asymptote
and the slope the drift. A second pass re-seeds the sigmoid on the
drift-corrected response, correcting an inflection biased by the drift.
A tail with fewer than two points seeds a zero drift. User-fixed values
are held.
}
\keyword{internal}
