% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyse_kinetics_helpers.R
\name{analyse_kinetics_channels}
\alias{analyse_kinetics_channels}
\title{Process kinetics fits across NIRS channels}
\usage{
analyse_kinetics_channels(
  data,
  nirs_channels,
  time_channel,
  per_channel,
  fit_fn,
  verbose = TRUE,
  interval_name = NA_character_,
  extra_args = list(),
  env = rlang::caller_env()
)
}
\arguments{
\item{data}{A single \emph{"mnirs"} data frame.}

\item{nirs_channels}{Character vector of resolved channel names.}

\item{time_channel}{Character; resolved time column name.}

\item{per_channel}{Named list (one element per channel) of resolved and
validated argument lists from \code{\link[=resolve_channel_args]{resolve_channel_args()}} and
\code{\link[=validate_kinetics_args]{validate_kinetics_args()}}.}

\item{fit_fn}{A function \verb{(.nirs, x_fit, t_fit, .a, valid, verbose)}
returning a list with \code{coefs} (1-row data frame of method coefficients,
\emph{without} \code{nirs_channels}/\code{time_channel}), \code{model}, \code{fitted_data}
(\code{window_idx}/\code{fitted}), and \code{diag} (1-row data frame from
\code{\link[=compute_diagnostics]{compute_diagnostics()}}).}

\item{verbose}{Logical. Default is \code{TRUE}. Display or silence (if \code{FALSE})
warnings and information messages helpful for troubleshooting. Ad
global default can be set via \code{options(mnirs.verbose = FALSE)}.}

\item{interval_name}{Character; the interval name recorded in the \code{interval}
column of the returned coefficients, \code{diagnostics}, and \code{channel_args}.}

\item{extra_args}{Named list of additional arguments recorded in the
\code{channel_args} result attribute.}

\item{env}{The calling environment or a defused call, used to report
errors and warnings as coming from the user-facing function rather
than the validator.}
}
\value{
A \code{data.frame} of coefficients (columns \code{interval},
\code{nirs_channels}, \code{time_channel}, and method parameters), one row per
channel, with attributes \code{"model"} and \code{"fitted_data"} (named lists by
channel) and \code{"diagnostics"} and \code{"channel_args"} (data frames, one row
per channel).
}
\description{
Shared per-channel skeleton for all \code{analyse_kinetics()} methods. Resolves
the fitting window for each channel via \code{\link[=find_kinetics_idx]{find_kinetics_idx()}}, delegates
the method-specific fit to \code{fit_fn}, and assembles the standard attributed
result. Method workers supply a validated \code{per_channel} argument list and a
\code{fit_fn}; everything else is common.
}
\keyword{internal}
