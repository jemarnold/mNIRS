% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyse_logistic.R
\name{SSlogistic}
\alias{SSlogistic}
\title{Self-starting logistic model}
\usage{
SSlogistic(t, A, B, xmid, slope, asym)
}
\arguments{
\item{t}{A numeric vector of the predictor variable (time).}

\item{A}{A numeric parameter for the starting asymptote of the response
variable.}

\item{B}{A numeric parameter for the ending asymptote of the response
variable.}

\item{xmid}{A numeric parameter for the \code{t} value at the inflection
(steepest) point of the curve, in units of the predictor variable \code{t}.}

\item{slope}{A numeric parameter for the slope \code{dx/dt} of the response
variable at the inflection point \code{xmid}.}

\item{asym}{A numeric parameter for the asymmetry index of the curve,
equal to the fraction of the response where the inflection point \code{xmid}
occurs, bounded in \code{c(0, 1)} for \code{(y(xmid) - A) / (B - A)}. \code{asym = 0.5}
is symmetric and equivalent to the 4-parameter form. If \code{NULL} (\emph{default}), a
symmetric 4-parameter model is used.}
}
\value{
A numeric vector of predicted values the same length as the
predictor variable \code{t}.
}
\description{
Creates initial coefficient estimates for a \code{selfStart} wrapper around
\code{\link[=logistic]{logistic()}}, for use with \code{\link[stats:nls]{stats::nls()}}. Supports both the 4-parameter
symmetric form (A, B, xmid, slope) and the 5-parameter asymmetric form
(A, B, xmid, slope, asym); arity is inferred from the formula passed to
\code{\link[stats:nls]{stats::nls()}}.
}
\details{
4-parameter model: \code{x ~ SSlogistic(t, A, B, xmid, slope)}

5-parameter model: \code{x ~ SSlogistic(t, A, B, xmid, slope, asym)}

The 4-parameter form is recommended for small samples or when no obvious
asymmetry is expected, as it converges more reliably. \code{\link[stats:nls]{stats::nls()}}
reads the free parameters from the formula right-hand side, so omitting
\code{asym} incurs no degrees-of-freedom penalty.
}
\examples{
## create a logistic curve with random noise
set.seed(15)
t <- 1:60
x <- logistic(t, A = 10, B = 100, xmid = 30, slope = 4, asym = 0.3) +
    rnorm(length(t), 0, 2)
data <- data.frame(t, x)

## 5-parameter fit
model5 <- nls(x ~ SSlogistic(t, A, B, xmid, slope, asym), data = data)
model5

## 4-parameter fit on the same data
model4 <- nls(x ~ SSlogistic(t, A, B, xmid, slope), data = data)
model4

y5 <- predict(model5, data)
y4 <- predict(model4, data)

\donttest{
    if (requireNamespace("ggplot2", quietly = TRUE)) {
        ggplot2::ggplot(data, ggplot2::aes(t, x)) +
            theme_mnirs() +
            ggplot2::geom_point() +
            ggplot2::geom_line(ggplot2::aes(y = y5, colour = "5-param")) +
            ggplot2::geom_line(ggplot2::aes(y = y4, colour = "4-param"))
    }
}

}
\seealso{
\code{\link[=logistic]{logistic()}}, \code{\link[stats:nls]{stats::nls()}}, \code{\link[stats:selfStart]{stats::selfStart()}},
\code{\link[stats:SSfpl]{stats::SSfpl()}}
}
