% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aanalyse_kinetics_helpers.R
\name{setup_kinetics_worker}
\alias{setup_kinetics_worker}
\title{Shared validation prologue for \verb{analyse_<method>()} workers}
\usage{
setup_kinetics_worker(
  data,
  nirs_quo,
  time_quo,
  arg_list,
  choices = list(),
  verbose = TRUE,
  env = rlang::caller_env()
)
}
\arguments{
\item{data}{A single \emph{"mnirs"} data frame.}

\item{nirs_quo, time_quo}{Quosures of the worker's \code{nirs_channels} and
\code{time_channel} arguments (captured with \code{enquo()} in the worker frame).}

\item{arg_list}{Named list of the method's per-channel-capable arguments.}

\item{choices}{Named list of valid values for choice-type arguments,
passed to \code{\link[=resolve_channel_args]{resolve_channel_args()}}.}

\item{verbose}{Logical. Default is \code{TRUE}. Display or silence (if \code{FALSE})
warnings and information messages helpful for troubleshooting. Ad
global default can be set via \code{options(mnirs.verbose = FALSE)}.}

\item{env}{The calling environment or a defused call, used to report
errors and warnings as coming from the user-facing function rather
than the validator.}
}
\value{
A named list with \code{nirs_channels}, \code{time_channel}, \code{t_vec}, and
\code{per_channel}.
}
\description{
Runs the identical per-interval setup shared by every kinetics worker:
validates \code{data}, resolves \code{nirs_channels} and \code{time_channel},
broadcasts global arguments across channels via \code{\link[=resolve_channel_args]{resolve_channel_args()}},
and validates the resolved per-channel arguments via
\code{\link[=validate_kinetics_args]{validate_kinetics_args()}}.
}
\keyword{internal}
