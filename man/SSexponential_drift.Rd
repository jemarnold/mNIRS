% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analyse_exponential_drift.R
\name{SSexponential_drift}
\alias{SSexponential_drift}
\title{Self-starting exponential-drift model}
\usage{
SSexponential_drift(t, A, B, tau, slope, tau_mult, TD)
}
\arguments{
\item{t}{A numeric vector of the predictor variable (time).}

\item{A}{A numeric parameter for the starting baseline of the response
variable.}

\item{B}{A numeric parameter for the ending asymptote of the response
variable.}

\item{tau}{A numeric parameter for the time constant \code{tau} (\eqn{\tau})
of the exponential curve, in units of the predictor variable \code{t}.}

\item{slope}{A numeric parameter for the linear drift rate \code{dx/dt}
of the secondary phase, in response units per unit of the predictor
variable \code{t}.}

\item{tau_mult}{A numeric multiple of \code{tau} after \code{TD} at which the linear
drift begins: excursion point; \code{texc = TD + tau_mult * tau} (\code{TD = 0}
when absent).}

\item{TD}{A numeric parameter for the time delay before the onset of
exponential response, in units of the predictor variable \code{t}. If \code{NULL}
(\emph{default}), a 3-parameter model without time delay is used.}
}
\value{
A numeric vector of predicted values the same length as the
predictor variable \code{t}.
}
\description{
Creates initial coefficient estimates for a \code{selfStart} wrapper around
\code{\link[=exponential_drift]{exponential_drift()}}, for use with \code{\link[stats:nls]{stats::nls()}}. Supports both the
5-parameter form (A, B, tau, slope, tau_mult) and the
6-parameter form adding a time delay TD; arity is inferred from the
formula passed to \code{\link[stats:nls]{stats::nls()}}.
}
\details{
5-parameter model:
\code{x ~ SSexponential_drift(t, A, B, tau, slope, tau_mult)}

6-parameter model:
\code{x ~ SSexponential_drift(t, A, B, tau, slope, tau_mult, TD)}

The hinge at \code{texc = TD + tau_mult * tau} is not differentiable, so
\code{algorithm = "port"} with \code{tau} (and \code{TD}) bounded non-negative and
\code{control = nls.control(warnOnly = TRUE)} is recommended.

The model function returns the analytic gradient (one-sided at the
hinge) for the free parameters as a \code{"gradient"} attribute, so
\code{\link[stats:nls]{stats::nls()}} does not resort to \code{\link[stats:numericDeriv]{stats::numericDeriv()}} and
\code{\link[stats:predict]{stats::predict()}} on a fitted model carries the attribute; drop it
with \code{as.vector()}.
\subsection{Fixing parameters}{

Any parameter may be held constant by writing a value in place of its
name in the formula, e.g.
\code{x ~ SSexponential_drift(t, A, B, tau, slope, tau_mult = 3)}
holds the drift onset at \code{3 * tau}. Fixed parameters are excluded from
estimation and are not returned by \code{\link[stats:coef]{stats::coef()}}.
}
}
\examples{
## create an exponential curve with late linear drift and random noise
set.seed(13)
t <- 1:180
x <- exponential_drift(
    t, A = 10, B = 100, tau = 12, slope = -0.5, tau_mult = 4, TD = 15
) + rnorm(length(t), 0, 2)
data <- data.frame(t, x)

## 6-parameter fit with the drift onset held at 4 tau
model <- nls(
    x ~ SSexponential_drift(t, A, B, tau, slope, tau_mult = 4, TD),
    data = data,
    algorithm = "port",
    lower = c(-Inf, -Inf, 0, -Inf, 0),
    control = nls.control(warnOnly = TRUE)
)
summary(model)

}
\seealso{
\code{\link[=exponential_drift]{exponential_drift()}}, \code{\link[stats:nls]{stats::nls()}}, \code{\link[stats:selfStart]{stats::selfStart()}},
\code{\link[=SSmonoexponential]{SSmonoexponential()}}
}
