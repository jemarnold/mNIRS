# wrap findInterval: informative 'time_channel' error message

wrap findInterval: informative 'time_channel' error message

## Usage

``` r
validate_findInt(x, vec, ..., env = rlang::caller_env())
```
